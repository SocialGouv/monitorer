{{ extend '../layout.art' }}

{{ block 'styles' }}
  <link href="/css/vendors/codemirror-5.51.0.min.css" rel="stylesheet">
  <link href="/css/vendors/codemirror-5.51.0.theme.ayu.min.css" rel="stylesheet">
  <link href="/css/admin.css" rel="stylesheet">
{{ /block }}

{{ block 'content' }}
  {{ if configuration }}
    {{ if isAdmin }}
      <h2>Configuration</h2>
      <textarea class="js-editor" readonly>{{ configuration.source }}</textarea>

      <h2 class="d-flex justify-content-between align-items-end">
        Maintenance
        <small class="subtitle-small text-monospace text-muted">
          Storage Size: {{ checkpointsSize }}
        </small>
      </h2>
      <table class="js-maintenance table table-borderless table-striped">
        <tbody>
          {{ each checkpoints }}
            <tr>
              <th scope="row">{{ $value.uri }}</th>
              <th class="text-right">{{ $value.length }}</th>
              <td class="table-buttonCell">
                <button
                  class="js-maintenance-cleanButton btn btn-warning btn-sm"
                  data-uri="{{ $value.uri }}"
                  title="Remove older-than-one-week history for: {{ $value.uri }}"
                  type="button"
                >
                  CLEAN
                </button>
              </td>
              <td class="table-buttonCell">
                <button
                  class="js-maintenance-dropButton btn btn-danger btn-sm"
                  data-uri="{{ $value.uri }}"
                  title="Remove the entire history for: {{ $value.uri }}"
                  type="button"
                >
                  DROP
                </button>
              </td>
            </tr>
          {{ /each }}
        </tbody>
      </table>
    {{ else }}
      <h2>Autentication</h2>
      <form class="js-form mt-2">
        <div class="form-group">
          <label>Username</label>
          <input class="form-control" type="text">
          <div class="invalid-feedback"></div>
        </div>
        <div class="form-group">
          <label>Password</label>
          <input class="form-control" type="password">
          <div class="invalid-feedback"></div>
        </div>
        <button type="submit" class="btn btn-primary">Log In</button>
      </form>
    {{ /if }}
  {{ else }}
    <div class="flex-grow-1 d-flex flex-column justify-content-center align-items-center">
      <div class="spinner bg-primary"></div>
      <div class="mt-4 text-muted">Initializing database…</div>
    </div>
  {{ /if }}
{{ /block }}

{{ block 'scripts' }}
  <script defer src="/js/vendors/codemirror-5.51.0.min.js"></script>
  <script defer src="/js/vendors/codemirror-5.51.0.mode.yaml.min.js"></script>
  <script defer src="/js/admin.js" type="module"></script>
{{ /block }}
