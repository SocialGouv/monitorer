extends:
  - "@socialgouv/eslint-config-react"

rules:
  no-console: ["error", { allow: ["info", "error", "warn"] }]
  react/prop-types: [0]

overrides:
  - files:
      - "jest.setup.js"
      - "*.test.js"

    env:
      jest: true
